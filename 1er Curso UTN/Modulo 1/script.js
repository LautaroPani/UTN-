let nombre = "Martin Fernando"; 

function name(params) {
    //Desarrollo de la Funcion
    return //palabra reservada para devolver lo que queramos
}

//Otra forma de escribir la funcion
let name =(params)=> {
    //Desarrollo de la Funcion
    return //palabra reservada para devolver lo que queramos
}

name(hola)

// VARIABLES: variables que solamente “existen” en algunas acciones en particulares,
// se dice que son variables LOCALES Estas variables locales se definen únicamente dentro de las funciones
//  que las necesitan, por lo que solamente van a existir cuando se ejecute dicha función. 
// Y las variables GLOBALES son aquellas variables que necesitamos utilizar en varias acciones distintas
//  y necesitamos que existan más allá de cualquier función, se llaman variables globales y se definen al 
// inicio del archivo JavaScript por fuera de cualquier función. Esto permite que podamos invocarlas
//  dentro de cualquier función y accionar según necesitemos. En cambio, las variables locales solamente
//  voy a poder invocarlas dentro de la misma función en la que se encuentran definidas.
